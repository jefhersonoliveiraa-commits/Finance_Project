// BottomNav removido — navegação mobile agora via drawer hamburger (AppLayout)
// Arquivo mantido vazio para não quebrar imports existentes
export function BottomNav(_props: { currentView?: string; onNavigate?: (v: string) => void }) {
  return null
}
